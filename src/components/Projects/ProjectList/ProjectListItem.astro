---
import type { ProjectMetadata } from "~/lib/projects";
import ProjectPreview from "../ProjectPreview.astro";

type Props = {
  p: ProjectMetadata;
};
const { p } = Astro.props;
---

<li data-reveal="bottom" data-easing="soft">
  <ProjectPreview project={p} />
</li>
