---
import { t } from "~/shared/i18n/fn";
import summary3 from "../../assets/media/summaries/summary-3.mp4";
import codeSummary from "../../assets/media/summaries/summary-code.mp4";
import designSummary from "../../assets/media/summaries/summary-design.mp4";
import performanceSummary from "../../assets/media/summaries/summary-performance.mp4";
import "./cards.css";
import { T } from "~/shared/i18n/types";
import StickyCard from "./sticky-card.astro";
const l = Astro.currentLocale;
---

<section id="cards" class="delayed-appearing mx-auto max-w-7xl px-6 xl:px-10">
  <div class="cards-container">
    <ul class="sticked-cards">
      <li class="card" id="card_1">
        <StickyCard
          title={t(l, T.card_1_title)}
          category="design"
          src={designSummary}
        />
      </li>
      <li class="card" id="card_2">
        <StickyCard
          title={t(l, T.card_2_title)}
          category="code"
          src={codeSummary}
        />
      </li>
      <li class="card" id="card_3">
        <StickyCard
          title={t(l, T.card_3_title)}
          category="publish"
          src={summary3}
        />
      </li>
      <li class="card" id="card_4">
        <StickyCard
          src={performanceSummary}
          title={t(l, T.card_4_title)}
          category="quality"
        />
      </li>
    </ul>
  </div>
</section>

<style>
  @keyframes fade-in {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  .cards-container {
    /* Create View Timeline */
    view-timeline-name: --revealing-images;
    view-timeline-axis: block;

    /* Attach animation, linked to the  View Timeline */
    animation: linear fade-in both;
    animation-timeline: --revealing-images;

    /* Tweak range when effect should run*/
    animation-range: normal 25%;
  }
</style>
